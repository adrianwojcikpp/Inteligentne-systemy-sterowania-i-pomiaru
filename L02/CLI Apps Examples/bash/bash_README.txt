In orderr to make the 'bash_HelloWorld.sh' file executable, enter this at the command prompt:

	sudo chmod +x bash_HelloWorld.sh

			^   ^ 
			|   |--- '+x': Adds execute permissions.
			|------- 'chmod': Command and system call for changing the access permissions of file system objects (files and directories).